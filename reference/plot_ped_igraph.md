# Render pedigree graph using Two-Pass strategy

Render pedigree graph using Two-Pass strategy

## Usage

``` r
plot_ped_igraph(
  g,
  l,
  node_size,
  gen_info = NULL,
  genlab = FALSE,
  genlabcex = NULL,
  ...
)
```
