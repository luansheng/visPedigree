# Access pedigree metadata from a tidyped object

Access pedigree metadata from a tidyped object

## Usage

``` r
pedmeta(x)
```

## Arguments

- x:

  A tidyped object.

## Value

The `ped_meta` list, or `NULL` if not set.
